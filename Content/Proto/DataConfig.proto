syntax = "proto3";
package pb;
option go_package = ".;pb";

message GuiConfig{
    message UIItem{
        int32 id = 1;
        string layout = 3;
        int32 layer = 4;
    }
    map<string, UIItem> item_map = 1;
}